<li>
	{{burger_name}}

	<button class="devour-burger btn btn-warning" data-id="{{id}}" data-newstate="{{devoured}}">
		{{#if devoured}} Send it back! {{/if}}
		{{#unless devoured}} Order! {{/unless}}
	</button>
</li>